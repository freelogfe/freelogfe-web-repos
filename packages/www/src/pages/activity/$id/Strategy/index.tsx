import * as React from 'react';
import styles from './index.less';

interface StrategyProps {

}

function Strategy({}: StrategyProps) {
  return (<div className={styles.strategy}>__Template</div>);
}

export default Strategy;
